
[project]
name = "sw403-p2"
version = "0.1.0"
description = "SW 403 RAG System - Prototype 2 (P2) cAST RAG with Qdrant"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    # Add any P2-specific dependencies here
]
